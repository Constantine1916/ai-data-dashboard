# 数据库连接字符串
# 格式: postgresql://username:password@host:port/database
# 示例: postgresql://user:pass@localhost:5432/ai_dashboard
# 生产环境请使用 Vercel 环境变量配置
DATABASE_URL=

# 应用 URL（可选，用于生成绝对 URL）
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node 环境
NODE_ENV=development

# JWT 密钥（生产环境必须设置，且要足够复杂）
JWT_SECRET=your-secret-key-change-in-production-min-32-chars

# JWT 过期时间（可选，默认 7d）
JWT_EXPIRES_IN=7d
